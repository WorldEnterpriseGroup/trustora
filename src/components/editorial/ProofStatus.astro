<section class="proof-status" aria-labelledby="proof-status-title">
  <div>
    <span class="section-label">Evidence in progress</span>
    <h2 id="proof-status-title">The proof should be real enough to stand behind.</h2>
  </div>
  <div class="proof-status-copy">
    <p>Trustora has five previous client relationships that are being prepared for public case studies. We will publish the scope, client label, outcomes, and quotations only after each client approves the details.</p>
    <a class="inline-link" href="/contact/">Request a substantiated reference conversation <span aria-hidden="true">↗</span></a>
  </div>
</section>

<style>
  .proof-status {
    display: grid;
    grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
    gap: 3rem;
    align-items: start;
    padding: 2rem 0;
    border-top: 1px solid rgba(255, 253, 248, 0.24);
    border-bottom: 1px solid rgba(255, 253, 248, 0.24);
  }

  .proof-status h2 {
    max-width: 29rem;
    margin: 0.7rem 0 0;
    color: var(--paper);
  }

  .proof-status-copy p {
    max-width: 42rem;
    margin: 0;
    color: rgba(255, 253, 248, 0.72);
  }

  .proof-status .inline-link {
    display: inline-flex;
    margin-top: 1.25rem;
    color: var(--paper);
  }

  @media (max-width: 700px) {
    .proof-status {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
  }
</style>
