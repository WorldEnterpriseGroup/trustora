image: node:22-bookworm-slim

stages:
  - verify
  - live-smoke
  - build
  - deploy
  - sync

variables:
  COREPACK_HOME: "$CI_PROJECT_DIR/.corepack"

cache:
  key:
    files:
      - pnpm-lock.yaml
  paths:
    - .pnpm-store

before_script:
  - mkdir -p "$CI_PROJECT_DIR/.corepack/bin"
  - corepack enable --install-directory "$CI_PROJECT_DIR/.corepack/bin"
  - export PATH="$CI_PROJECT_DIR/.corepack/bin:$PATH"
  - corepack prepare pnpm@11.21.0 --activate
  - pnpm install --frozen-lockfile

verify:
  stage: verify
  image: mcr.microsoft.com/playwright:v1.62.1-noble
  script:
    - test -n "$PUBLIC_CAREERS_API_URL"
    - test -n "$PUBLIC_TRUSTORA_INTAKE_API_URL"
    - pnpm run check
    - pnpm run build
    - pnpm run test
    - pnpm run audit:technical
    - pnpm exec playwright install chromium
    - pnpm run qa:browser

live-intake-smoke:
  stage: live-smoke
  needs:
    - job: verify
      artifacts: false
  script:
    - test -n "$D365_SMOKE_TOKEN"
    - pnpm run qa:live
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
    - when: never

build:
  stage: build
  script:
    - pnpm run build
  artifacts:
    paths:
      - dist
    expire_in: 1 day

pages:
  stage: deploy
  needs:
    - job: build
      artifacts: true
    - job: live-intake-smoke
      artifacts: false
  script:
    - rm -rf public
    - mkdir -p public
    - cp -a dist/. public/
    # Astro does not emit the repository-root custom-domain marker.
    - cp CNAME public/CNAME
    - |
      for forbidden in '.claude' '.mcp.json' 'CLAUDE.md' 'plan.md' '.gitlab-ci.yml' 'docs' 'package.json' 'pnpm-lock.yaml'; do
        if [ -e "public/$forbidden" ]; then
          echo "Forbidden repository file copied into Pages artifact: $forbidden"
          exit 1
        fi
      done
  pages: true
  artifacts:
    paths:
      - public
    expire_in: never
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'

sync-github:
  stage: sync
  image: debian:bookworm-slim
  before_script:
    - apt-get update
    - apt-get install -y --no-install-recommends ca-certificates git openssh-client
  needs:
    - job: pages
      artifacts: false
  script:
    - test -n "$GITHUB_SYNC_KEY_B64"
    - install -m 700 -d "$CI_PROJECT_DIR/.sync-ssh"
    - printf '%s' "$GITHUB_SYNC_KEY_B64" | base64 --decode > "$CI_PROJECT_DIR/.sync-ssh/id_ed25519"
    - chmod 600 "$CI_PROJECT_DIR/.sync-ssh/id_ed25519"
    - ssh-keyscan -H github.com > "$CI_PROJECT_DIR/.sync-ssh/known_hosts"
    - export GIT_SSH_COMMAND="ssh -i $CI_PROJECT_DIR/.sync-ssh/id_ed25519 -o IdentitiesOnly=yes -o UserKnownHostsFile=$CI_PROJECT_DIR/.sync-ssh/known_hosts -o StrictHostKeyChecking=yes"
    - git remote add github-sync git@github.com:WorldEnterpriseGroup/trustora.git
    - git push github-sync "HEAD:gh-pages"
    - synced_sha="$(git ls-remote github-sync refs/heads/gh-pages | awk '{print $1}')"
    - test "$synced_sha" = "$CI_COMMIT_SHA"
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
    - when: never
